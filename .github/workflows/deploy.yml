name: Deploy Glance Config

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull and restart Glance
        run: |
          cd /opt/glance
          git pull origin main
          systemctl restart glance